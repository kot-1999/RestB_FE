div.dashboard-page
  .bookings-layout-container
    // Left Sticky Sidebar - Aligned to top
    aside.home-sidebar
      .home-sidebar-panel
        .home-sidebar-head
          h1 RestBoo
        p.home-sidebar-copy Filter bookings.

        .home-filter-group
          label.home-filter-label(for="filter-from") START DATE
          input#filter-from.home-input(type="date")

        .home-filter-group
          label.home-filter-label(for="filter-to") END DATE
          input#filter-to.home-input(type="date")

        .home-filter-group
          label.home-filter-label(for="filter-status") STATUS
          select#filter-status.home-select
            option(value="") All Statuses
            option(value="Pending") Pending
            option(value="Approved") Approved
            option(value="Completed") Completed
            option(value="Cancelled") Cancelled
            option(value="NoShow") No Show

        button#apply-filters.home-submit-btn APPLY FILTERS

    // Right Content Area - Contains header and cards
    main.bookings-main-content
      .dash-header
        .dash-title
          h1#dashboardTitle Bookings for <span style="color:var(--accent-white)" id="restaurant-name"></span>
          p#dashboardSub Today's mission control overview.

        // Brand component inside the header
        div.dash-brand-minimal#brand-container
          .dash-brand-label BRAND
          .dash-brand-logo
            img.js-brand-img(src="/assets/img/default-avatar.png" alt="Loading...")
          .dash-brand-name Loading...

      // Bookings grid outside the header
      .bookings-grid#bookings-list
        // Booking cards will be rendered here by JavaScript
